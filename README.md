DDC and TAFF definition codes are in the densenet file.
Transformer related code is in the decoder file.
The application location of each module is in the CAN model file, and the relevant parameters are set in the config file. 
Once the data set and environment are ready, they can be run directly in the console.
